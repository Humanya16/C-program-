#include <stdio.h>
#include<math.h>
int  main() 
{
    int n,i=1,mul;
    printf("enter the n value:");
    scanf("%d",&n);
   while(i<=10)
   {
        mul=n*i;
        i++;
    }
    printf("%d*%d=%d\n",n,i,mul);
}
