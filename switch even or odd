#include<stdio.h>
int main()
{
    int a;
   printf("enter the number:" );
   scanf("%d",&a);

    switch(a%2)
    {
        case 1: printf("the number is even");
                break;
         case 2: printf("the number is odd");
                break;
    
    
    }
}
